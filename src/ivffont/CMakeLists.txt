IF(IVFWIN32)
	INCLUDE_DIRECTORIES(${ivf_SOURCE_DIR}/include)
ELSE(IVFWIN32)
	INCLUDE_DIRECTORIES(${ivf_SOURCE_DIR}/include ${FREETYPE_INCLUDE})
ENDIF(IVFWIN32)

FILE(GLOB SOURCE_FILES *.cpp)
FILE(GLOB INCLUDE_FILES ${ivf_SOURCE_DIR}/include/ivffont/*.h)

IF(BUILD_SHARED_LIBS)
	ADD_DEFINITIONS(-DIVF_DLL -DIVFFONT_LIB )
	IF(IVFWIN32)
		SET(FONT_LIBS ftgl_dynamic_MTD freetypeMT_imp )
		SET(FONT_DEBUG_LIBS ftgl_dynamic_MTD_d freetypeMT_D_imp )
	ELSE(IVFWIN32)
		SET(FONT_LIBS ftgl freetype )
	ENDIF(IVFWIN32)		
ELSE(BUILD_SHARED_LIBS)
	IF(IVFWIN32)
		SET(FONT_LIBS ftgl_static_MTD freetype221MT )
		SET(FONT_DEBUG_LIBS ftgl_static_MTD_d freetype221MT_D )
	ELSE(IVFWIN32)
		SET(FONT_LIBS ftgl freetype )
	ENDIF(IVFWIN32)	
ENDIF(BUILD_SHARED_LIBS)

IF(IVFWIN32)
	IF(IVFDEBUG)
		ADD_LIBRARY(ivffontd ${INCLUDE_FILES} ${SOURCE_FILES})
		TARGET_LINK_LIBRARIES( ivffontd ivfd ${FONT_DEBUG_LIBS} )
		INSTALL_TARGETS( /lib ivffontd )
	ELSE(IVFDEBUG)
		ADD_LIBRARY(ivffont ${INCLUDE_FILES} ${SOURCE_FILES})
		TARGET_LINK_LIBRARIES( ivffont ivf ${FONT_LIBS} )
		INSTALL_TARGETS( /lib ivffont )
	ENDIF(IVFDEBUG)
ELSE(IVFWIN32)
	ADD_LIBRARY(ivffont ${SOURCE_FILES})
	TARGET_LINK_LIBRARIES( ivffont ivf ${FONT_LIBS})
	INSTALL_TARGETS( /lib ivffont )
ENDIF(IVFWIN32)


